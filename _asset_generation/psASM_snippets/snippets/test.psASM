#
# std_string.psASM
# Part of StdLib for psMCU.
# Contains macros to save string literals to ram/stack.
#

@macro ascii_heap $str, $adr
    pointless_label, l5: PUSHA
    @for $i, 0, $i<strlen($str), $i+1
        LITA $str[$i] 
        SVA $adr+$i
    @end
    POPA
@end
